- if !@wages.empty?
  - wage = @wages.first
  .row
    %h4.small-6.columns.left
      = "#{wage.FY}: Week #{wage.week_no}"
  .row.headings
    .small-1.columns
      Name
    .small-1.columns
      Time
    .small-1.columns
      Hourly
    .small-1.columns
      Tips
    .small-1.columns
      Total
    .small-1.columns.lefty
      PAYE
    .small-1.columns.lefty
      NI E'ee
    .small-1.columns.left.lefty
      NI E'er
    .small-3.columns.left.lefty
      Paid
  - @wages.each do |wage|
    - @wage = wage
    = render 'wage'
-else
  No timesheets for Period

:coffeescript
  Ajaxify.set_content_container('daily_editor')