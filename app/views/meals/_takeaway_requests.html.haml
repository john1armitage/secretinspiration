- @meals = Meal.where("seating_id IS NULL AND state = 'requested'").order('start_time')
- unless @meals.empty?
  %ul.aside.no-print
    %li.title
      %span
        Takeaway Requests
    - @meals.each do |meal|
      = render 'meals/meal', meal: meal
