%ul.side-nav
  - if allow? :users, :index
    %li
      != link_to 'Users', users_path, :class => "small button radius"
  - if allow? :roles, :index
    %li
      != link_to 'Roles', roles_path, :class => "small button radius"
  - if allow? :tenancies, :index
    %li
      != link_to 'Tenancies', tenancies_path, :class => "small button radius"
  %li.divider
  - if allow? :topics, :index
    %li
      != link_to 'Topics', topics_path, :class => "small button radius"
  %li.divider
  - if allow? :elements, :index
    %li
      != link_to 'Elements', elements_path, :class => "small button radius"
  - if (current_tenant.sitting && current_tenant.sitting == '1') && (allow? :tabels, :index )
    %li
      != link_to 'Tables', tabels_path, :class => "small button radius"
  - if allow? :items, :index
    %li
      != link_to 'Items', items_path, :class => "small button radius"
  - if allow? :item_types, :index
    %li
      != link_to 'Item Types', item_types_path, :class => "small button radius"
  - if allow? :categories, :index
    %li
      != link_to 'Categories', categories_path, :class => "small button radius"
  - if allow? :item_fields, :index
    %li
      != link_to 'Item Fields', item_fields_path, :class => "small button radius"
  - if allow? :dailies, :delete
    - months = get_months((Date.today.beginning_of_month - 2.year), 25).reverse
    %li
      =# select_tag 'balance', options_for_select(months, Date.today.beginning_of_month)
      != link_to 'Test', dailies_path(daily_date: Date.today.beginning_of_month, calendar: false, balance: true), :class => "small button radius no_ajaxify", remote: true
